values:
  test:
    configmaps: ofek
    images:
      tag: latestofek-policy
values:
  test:
    configmaps: refael
    images:
      tag: latestrefael-policy
values:
  test:
    configmaps: matan
    images:
      tag: latestmatan-policy
values:
  test:
    configmaps: shoval
    images:
      tag: latestshoval-policy
values:
  test:
    configmaps: shavit
    images:
      tag: latestshavit-policy
values:
  test:
    configmaps: dor
    images:
      tag: latestdor-policy
values:
  test:
    configmaps: ofek
    images:
      tag: latestofek-policy
values:
  test:
    configmaps: refael
    images:
      tag: latestrefael-policy
values:
  test:
    configmaps: matan
    images:
      tag: latestmatan-policy
values:
  test:
    configmaps: shoval
    images:
      tag: latestshoval-policy
values:
  test:
    configmaps: shavit
    images:
      tag: latestshavit-policy
values:
  test:
    configmaps: dor
    images:
      tag: latestdor-policy
values:
  test:
    configmaps: ofek
    images:
      tag: latestofek-policy
